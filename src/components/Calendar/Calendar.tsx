import React from "react";
import Calendar from "react-calendar";
import "react-calendar/dist/Calendar.css";

function CalendarComponent({ ragloads }) {
	return (
		<div>
			<Calendar />
		</div>
	);
}

export default CalendarComponent;
