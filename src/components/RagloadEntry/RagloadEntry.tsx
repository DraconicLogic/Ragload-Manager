/**
 * I Want this screen when closed to return to the previous screen.
 */

function RagloadEntry() {
	return (
		<div id="RagloadEntry">
			<h1>Ragload Entry</h1>
		</div>
	);
}

export default RagloadEntry;
